function Hero() {
  return (
    <section style={{ padding: 40 }}>
      <h1 id="hero-heading">Vite + React hero</h1>
      <p id="hero-para">Edit me via frontman-flow.</p>
      <button id="hero-btn" type="button">
        A button
      </button>
    </section>
  );
}

export default function App() {
  return <Hero />;
}
