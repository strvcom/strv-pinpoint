# Read-only upstream reference, re-cloned on demand. Never committed.
/.reference/

node_modules/
dist/
.claude/worktrees/

# macOS Finder metadata
.DS_Store

# Local runtime artifacts (overlay/bridge session data, skill scratch)
/.frontman-flow/
/.superpowers/

# built plugin bridge bundle
packages/claude-code/bin/frontman-flow
