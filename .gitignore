node_modules/
dist/
.claude/worktrees/

# macOS Finder metadata
.DS_Store

# Local runtime artifacts (overlay/bridge session data, skill scratch)
/.pinpoint/
/.superpowers/

# Transient release notes written by the Release workflow (consumed by gh, never committed)
/RELEASE_NOTES.md
